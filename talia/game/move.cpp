#include "move.h"

/* ---------------------------------------------------------------------------- */

Move::Move(): from(X), to(X), piece(Empty), player(None), promotion(Empty), midway(X), side(Neither), flag(Null) {}

/* ---------------------------------------------------------------------------- */