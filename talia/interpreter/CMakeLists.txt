#add_library(cli STATIC cli.cpp)

#include_directories(${CMAKE_SOURCE_DIR}/../interface)

#target_include_directories(cli PUBLIC ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_SOURCE_DIR}/../interface)

#target_link_libraries(cli PUBLIC config)